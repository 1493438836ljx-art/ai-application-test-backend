<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.chat.mapper.ChatFeedbackMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.demo.chat.entity.ChatFeedbackEntity">
        <id column="id" property="id"/>
        <result column="message_id" property="messageId"/>
        <result column="user_id" property="userId"/>
        <result column="rating" property="rating"/>
        <result column="feedback_type" property="feedbackType"/>
        <result column="comment" property="comment"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, message_id, user_id, rating, feedback_type, comment, created_at
    </sql>

    <!-- 根据消息ID查询反馈 -->
    <select id="selectByMessageId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM chat_feedback
        WHERE message_id = #{messageId}
        LIMIT 1
    </select>

    <!-- 根据用户ID查询反馈列表 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM chat_feedback
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

</mapper>
